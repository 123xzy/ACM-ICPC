#include<iostream>
#include<cstring>
#include<utility>
using namespace std;

#define MAXN 1005
char ans[MAXN];

int main()
{
	int T; cin >> T;
	while (T--)
	{
		pair<char, int> p('A', 0);
		p.insert()
		int n, m;
		cin >> m >> n;
		for (int i = 0; i < m; i++)
		{
			cin >> dna;
			for (int j = 0; j < n; j++)
			{
				if (dna[j] == 'A')letter_num[i].A++;
				else if (dna[j] == 'C')letter_num[i].C++;
				else if (dna[j] == 'G')letter_num[i].G++;
				else if (dna[j] == 'T')letter_num[i].T++;
			}
		}


	}
}
